---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ portalname }}-pvc"
  namespace: "{{ namespace }}"
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
