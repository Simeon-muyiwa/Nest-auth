---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: '{{ portalname }}-pvc'
  namespace: '{{ namespace }}'
spec:
  storageClassName: 'manual'
  volumeName: '{{ portalname }}-pv'
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
