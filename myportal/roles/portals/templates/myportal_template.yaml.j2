apiVersion: portal.mycourse1.com/v1
kind: MyPortal
metadata:
  name: "{{ item.mytenantname }}"  # Ensure this is compliant
  namespace: "{{ item.namespace }}"
spec:
  myportals:
    {% for portal in item.portals %}
    - 
      
      name: "{{ portal.name }}"
      replicacount: {{ portal.replicacount }}
    {% endfor %}
 mytenantname: "{{ item.mytenantname }}"